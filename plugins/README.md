# Plugins

Platform integrations live in separate folders under this directory. They read SoundGIF data but
do not own the file format or encoder.

Current integration:

- `vencord/soundGif`: Discord playback through Vencord.
